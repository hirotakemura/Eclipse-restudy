# 見た目の検査に使う写真（架空）

**お客様の写真ではありません。** 無地に対角線を引いただけの画像で、
中身に意味はありません。

`.svg` の仮の画像は `analyze()` が写真として数えないため（D-174）、
**「写真が届いた状態」を検証できませんでした**（D-288）。
そのため実ファイル形式（PNG）で用意しています。

案件データ側では `mock: true` を付けているので、**公開判定は止まります**（D-242）。
