# 第7段階 設計レビュー — 余白・タイポグラフィ・アートディレクション

**これは設計案である。コードはまだ1行も変えていない。**

課題は「情報は正しいのに、テンプレートに見える」。
**色でも装飾でもなく、構造として原因を説明できる状態にする**のが、この文書の目的である。

---

## 0. 結論から

測った結果、原因は3つの数字に集約できる。

| いま | 意味 |
|---|---|
| **見出し ÷ 本文 ＝ 1.18倍** | 帯の見出しが本文より2割大きいだけ。**階層が無い** |
| **最大字の並び 44 → 20 → 28 → 20 → 20** | 山のあとが**平坦**。ページが減衰して終わる |
| **中身が使う幅は 760 と 1070 の2種類だけ** | 横の変化が無い。**目が左右に動かない** |

**余白を増やしても、これは直らない。** 直すのは「差」である。

---

## A. 全15方向の実測（写真0枚・PC 1280px・トップページ）

製造業は「難加工が強い会社」、汎用は「ヘアサロン」の検証データで、型だけを差し替えた。

| 型 | 帯の余白(px) | 高さ比 | 面数 | 組方数 | 密度数 | 見出し | 本文 | 行長 | 山 |
|---|---|---|---|---|---|---|---|---|---|
| standard | 60/72/96 | 2.3 | 2 | 2 | 3 | 20/28/44 | 17 | 680 | 49 |
| technical | 36/60/72/96 | 2.3 | 3 | 3 | 4 | 12/20/28/44 | 17 | 680 | 49 |
| craft | 60/72/96 | 1.8 | 2 | 2 | 3 | 28/44 | 17 | 680 | 60 |
| engineering | 60/72/96 | 2.5 | 2 | 3 | 3 | 20/28/44 | 17 | 680 | 44 |
| industrial | 36/60/72/96 | 2.1 | 3 | 2 | 4 | 12/20/28/44 | 17 | 680 | 49 |
| product | 60/96 | 1.9 | 3 | 3 | 2 | 28/44 | 17 | 680 | 60 |
| seikatsu | 88/106 | 1.9 | 2 | 2 | 2 | 20/28/44 | 17 | 680 | 49 |
| shop | 88/106 | 1.9 | 3 | 2 | 2 | 20/28/44 | 17 | 680 | 49 |
| gstandard | 88/106 | 1.9 | 2 | **1** | 2 | 28/44 | 17 | 680 | 49 |
| editorial | 88/106 | 2.0 | 2 | 2 | 2 | 28/44 | 17 | 680 | 60 |
| luxury | 88/106 | 2.0 | 2 | 2 | 2 | 28/44 | 17 | 680 | 60 |
| modern | 88/106 | 1.9 | 3 | 3 | 2 | 20/28/44 | 17 | 680 | 60 |
| human | 88/106 | 1.9 | 2 | 2 | 2 | 20/28/44 | 17 | 680 | 49 |
| dynamic | 88/106 | 1.9 | 2 | 3 | 2 | 20/28/44 | 17 | 680 | 60 |
| classic | 88/106 | 2.0 | 2 | 2 | 2 | 28/44 | 17 | 680 | 49 |

### 下層ページ（PC）

| | 全15型 |
|---|---|
| ページ頭の余白 | **40/40（15型すべて同じ）** |
| ページ見出し h1 | **33px（15型すべて同じ）** |
| 表の詰め | **12/14（15型すべて同じ）** |
| カードの間 | **16px（全型同じ）** |
| 帯の余白 | 製造業 60/96　汎用 106/141 |

### スマホ（390px）

| | 全15型 |
|---|---|
| 帯の余白 | 製造業 60/69/84（＋詰め42）　汎用 **88/101** |
| 見出し | 19/21/24 |
| 本文 | **16px（全型同じ）** |
| 山 | 26〜32px |
| 見出しの左端 | **16px（全型同じ）** |

**スマホでは、余白の差が 88 と 101（1.15倍）しかない。** PCより階層が浅い。

### 型による違いは、実質2つしかない

| 軸 | 取りうる値 | 効き |
|---|---|---|
| `mood` → `--section` | 60 / 72 / 88px | 帯の余白の基準 |
| `textSize` → `--font-size` | 17 / 18 / 20px | 本文（**差は1px**） |

**タイポグラフィの段（`TYPE_ROLES` の11役割）は、15方向で完全に同一である。**
型は「面・地紋・組み方の候補」を変えるが、**余白の比も文字の比も変えていない。**

---

## B. なぜテンプレートに見えるか（構造として）

松原精機のトップページと強みページを、帯ごとに測った。

```
トップ    最大字 44 → 20 → 28 → 20 → 20    余白 141 → 88 → 88 → 106 → 88
強み      最大字 44 → 28 → 20 → 20         余白 141 → 88 → 88 → 53
中身の幅  760 / 1070 の2種類だけ
```

### B-1 見出しと本文の差が弱い（最大の原因）

帯の見出しは **20px または 28px**、本文は **17px**。比は **1.18〜1.65倍**。
山だけが 44px（2.59倍）ある。

つまり階層は**「山」と「平地」の2段**しかない。
`TYPE_ROLES` には11の役割があるのに、**画面に出ているのは実質3つ**である。

とくに `split`（一覧の既定の組み方）は、見出しを **19〜24px に固定**している。
これは D-239 の判断（狭い左列で大きな見出しは語の途中で折れる）で**正しい**。
しかし結果として、**一覧の帯はすべて 20px の見出しになる。**

### B-2 山のあとが平坦

`44 → 20 → 28 → 20 → 20`。
山が上から1/3にあり、**そのあとページは減衰するだけ**で、終盤に何の山もない。
最後の帯（設備の表）と、その前の帯（材質の札）と、その前（事例）が、**同じ視覚的重さ**で並ぶ。

### B-3 余白の差が小さい

密度の倍率は `tight 0.6 / normal 1.0 / loose 1.2 / vast 1.6`。
実寸で **53 / 88 / 106 / 141**。
`normal` と `loose` の差は **18px** で、**画面では差として読めない。**
しかも5帯のうち3帯が同じ 88px である。

### B-4 横の変化が無い

中身が使う幅は **760 と 1070 の2種類**。
見出しの左端は **230 か 385 の2箇所**だけ。
**目が上下にしか動かない。** 余白は「構成した余白」ではなく「余った場所」になっている。

### B-5 面が2種類

`plain` と `rule`（または `soft`）の2つ。
章の区切りは罫線1本。**背景の切り替わりで場面が変わる感じが無い。**

### B-6 「カードが並んでいるだけ」の正体

実際にはカードは少ない（トップページに格子は0）。
問題は**カードの数ではなく、帯の形がすべて同じ**ことである。

```
見出し（20px）→ 中身（17px）→ 余白88px →
見出し（20px）→ 中身（17px）→ 余白88px → …
```

**同じ部品が同じ間隔で積まれている。** これがテンプレートに見える正体である。

### B-7 写真0枚のとき、仮画像が一番大きい視覚物になる

実測：トップページで最も大きい面積を占めるのは **1030×500 の灰色の仮画像**で、
そこに「仮の画像（外観）／差し替え前提」と書いてある。
**写真が無い会社ほど、画面の主役が「無い写真」になる。**

---

## C. 15方向それぞれの視覚原則（1〜3個ずつ）

**いまの実装（面・組み方・地紋・気分）から導いたもの**で、新しい語彙は増やしていない。

### 製造業

| 型 | 視覚原則 |
|---|---|
| **standard** 標準 | ①一本道の読み順（`stack` 中心）②罫線で章を切る ③**差をつけない**——迷ったときの安全な既定 |
| **technical** 精密加工 | ①**値が見出しより大きい**（数値の階層）②方眼・寸法線の規律＝要素を格子に乗せる ③行間を詰める（`katai`） |
| **craft** 老舗・職人 | ①**紙の地と読み物の余白**（`paper` + `editorial`）②**非対称**（`offset`）③人の言葉を大きく |
| **engineering** 設計・技術 | ①**正確な整列**（`split` の左見出しを軸線として使う）②工程・断面の注記 ③図と本文が同格 |
| **industrial** 量産・設備 | ①**暗い面で締める**（`dark`）②**全幅で数と体制を見せる**（`fullbleed`）③章が交互に切り替わる |
| **product** 製品・開発 | ①**製品を全幅で主役にする** ②やわらかい余白（`yawaraka`）③見出しは下線で軽く |

### 汎用

| 型 | 視覚原則 |
|---|---|
| **seikatsu** 生活サービス | ①やわらかい面が交互に来る ②人が前に出る ③一本道 |
| **shop** 店舗・サービス | ①**アクセント面で価格を強調** ②表を主役に |
| **gstandard** 標準 | ①**何もしない。** 最小構成であることが原則 |
| **editorial** 読み物 | ①**狭い本文・広い余白** ②見出しを大きく ③非対称 |
| **luxury** 静か・上質 | ①**要素を減らす** ②**余白そのものが主役**（帯の間を最大に）③装飾を置かない |
| **modern** モダン | ①**格子**（`grid` 地紋＋`split`）②大きな文字 ③ミニマル |
| **human** 人・温度 | ①**写真を全幅で主役に** ②やわらかい面 ③文字大きめ |
| **dynamic** 力強い | ①**暗い面と大きな文字で締める** ②全幅 ③見出しを帯に |
| **classic** 落ち着き・信頼 | ①**紙の地** ②控えめな装飾と上品な余白 ③文字大きめ |

**共通して言えること**：15方向のうち **9方向の原則が「余白の取り方」か「文字の大きさ」**で表せる。
つまり第7段階で足すべきなのは**装飾ではなく、余白と文字の段**である。

---

## D. 共通化するもの／方向ごとに変えるもの

| | いま | あるべき |
|---|---|---|
| **余白の比**（tight:normal:loose:vast） | 共通・0.6:1:1.2:1.6 | **共通**。ただし**比を広げる** |
| **余白の基準値**（`--section`） | 型ごと（60/72/88） | **型ごと**（現状維持） |
| **文字の役割**（11役割） | 共通 | **共通**（増やさない） |
| **文字の比**（見出し÷本文） | 共通・実質1.18 | **共通の段を深くし、強さだけ型ごと** |
| **本文の幅**（`--measure`） | 共通・40em | **共通** |
| **中身の幅の段** | 2種類（760/1070） | **共通の4段**（狭/標準/広/全幅） |
| **面の切り替え方** | 型ごと（line/space/alternate） | **型ごと**（現状維持） |
| **山の構図** | 山の種類ごと | **型ごとの重みを足す** |
| **見出しの位置** | 2箇所のみ | **型ごと**（左寄せ／横置き／中央） |
| **装飾の幾何** | 型ごと（Asset層） | **型ごと**（現状維持） |
| **画像の扱い** | 共通 | **型ごと**（全幅／枠内／比率） |

---

## E. 原則（数値を決める前に）

1. **階層は「山」ではなく「段」で作る。**
   いま 山→平地 の2段しかない。**eyebrow / 見出し / 導入 / 本文 / 注記**の段を実際に画面へ出す。
2. **ページは山のあとにも段を持つ。**
   終盤に**もう1つだけ**小さな山（値か一言）を許す。トップだけ豪華にしない。
3. **余白は比で設計する。** 絶対値は型（`mood`）が決め、**比は共通**。
   いまの 0.6:1:1.2:1.6 は差が小さすぎる。
4. **横の段を増やす。** 幅が2種類では構図が作れない。
5. **型ごとの違いは「段の強さ」2つだけにする。**
   新しい軸を増やさず、**文字の強さ**と**余白の強さ**の2パラメータで表す。
6. **スマホを先に決める。** いま PC より階層が浅い。
7. **写真が無いときの主役を、仮画像にしない。**

---

## F. 最小変更の単位（この順で）

**どれも独立して入れられて、独立して戻せる。**

| # | 変更 | 触る場所 | 期待 |
|---|---|---|---|
| 1 | **余白の比を広げる**（0.6:1:1.2:1.6 → 0.5:1:1.5:2.2） | `density.ts` の4行 | 余白が「差」として読める |
| 2 | **`sectionTitle` を上げ、`lead` を実際に使う** | `typography.ts` の2行＋`site.css` | 見出し÷本文 1.18 → 1.6〜1.8 |
| 3 | **`split` の見出しを eyebrow として組む**（小さい見出しではなく、字間を空けた項目名として） | `site.css` の1規則 | 20pxの見出しが「小さい」ではなく「意図」に見える |
| 4 | **中身の幅を4段にする**（狭680/標準900/広1070/全幅） | `site.css` の `data-width` | 横の変化が出る |
| 5 | **ページ終盤に2つ目の小さな山を許す** | `visual.ts` の山の選び方 | 減衰しなくなる |
| 6 | **型に「段の強さ」2つを足す** | `direction.ts` に2フィールド | 15方向の差が余白と文字に出る |
| 7 | **仮画像を山にしない** | `visual.ts` の `peakFor`（`hasRealPhotos` を見る） | 写真0枚の画面が締まる |

**1〜3だけで、B-1 と B-3 は解消する見込み。** まずそこで測り直したい。

---

## G. 確認する場面（実装のとき）

いまの実測は**写真0枚のトップページ**に偏っている。実装時は次を全部見る。

- 製造業 A/B/C ／ 汎用 A/B/C
- **全15方向**（D-329の再発防止）
- 写真 0枚 / 1枚 / 多数
- 事例 0件 / 1件 / 4件
- 設備 複数
- 採用あり / なし
- PC / スマホ **両方**（スマホを後回しにしない）
- 第6.5の `keepAll` と原則C が緑のまま

**成功条件は「豪華になったか」ではない。**
**同じ情報量のまま、見出し÷本文の比・余白の比・幅の段が、画面で読めるようになること**である。

---

# 実装記録（第7段階・1/2/3/7 のみ）

アドバイザーの判断で **1・2・3・7 の4点だけ**を1つの実装単位として入れた。
**4（幅の段）・5（2つ目の山）・6（型に段の強さ）は保留。**

## 1. 余白の比を広げた

```
前 0.6 : 1.0 : 1.2 : 1.6   （最大÷最小 2.7倍）
後 0.5 : 1.0 : 1.5 : 2.2   （同 4.4倍）
スマホ 0.7:1:1.15:1.4 → 0.6:1:1.3:1.7
```

**余白を増やしたのではない。差を広げた。** `normal` は動かしていないので、
大半の帯の見え方は変わらない——変わるのは「続けて読ませる帯」と「山の前後」だけである。

### ここで1つ、書き写した表に引っかかった

倍率は `density.ts` と `site.css` の**2箇所**に書いてあった（`calc(var(--section) * 1.6)`）。
`density.ts` だけを直したとき、**画面の余白は1pxも変わらなかった。**
突き合わせの試験（`visual.test.mjs`）は前からあり、CSSを直すまで赤いままだった。
**D-197 の3度目**である。

## 2. 見出しと導入の段を、実際の画面に出した

| 役割 | 前 | 後 | 1280pxでの実寸 |
|---|---|---|---|
| `sectionTitle` | min21 / 2.2vw / max34 | min22 / **2.6vw** / max38 | 28 → **33px** |
| `lead` | min18 / 1.4vw / max22 | min18 / **1.7vw** / max25 | 18 → **24px** |

**導入の色を戻した。** 実測で、導入は 19px・`--ink-soft`（**6.0:1**）で、
**ページで最も重要度の高い文のひとつが、いちばん薄い文字**だった。
薄さは「控えめ」ではなく「読みにくい」として届く。

**薄い文字を一律に濃くしたのではない。重要度の順に並べ直した。**

| | 色 |
|---|---|
| 見出し・導入・本文・沿革の出来事 | `--ink` |
| 項目名・注記・説明・年・メーカー名・キッカー | `--ink-soft`（据え置き） |

`--ink-soft` を使う箇所は**17箇所のうち2箇所だけ**動かした（`.lead` と `.timeline dd`）。

## 3. 横置きの見出しを「項目名」として組み直した

一覧の帯は既定で `split` になるため、**見出しが軒並み 20px**（本文の1.18倍）だった。

**大きくして直す道は取らなかった。** 狭い左列で大きな見出しは
「対応できる条／件」のように語の途中で折れる（D-239。この判断は変えない）。

直したのは**役割**である。1.18倍の見出しは「小さい見出し」に見えるが、
**字間を空けた項目名にすると、そういうものとして読める。**
語彙は増やしていない——`label`（項目名）は前からある役割である。

### ここでも画面を見て直した

最初は `--t-label`（12px）＋`--ink-soft`（6.16:1）にした。**画面で見たら弱すぎた。**
中身との間が **318px** 空いているので、薄い12pxは見出しではなく「余りもの」に見えた。
**小さくするのはよいが、薄くするのは別の話だった。**
`--t-caption`（14px）＋`--ink`（15.92:1）に直した。

## 7. 最初の画面に仮画像を置かない

**実測：写真0枚の会社のトップページで、画面で最も大きい視覚物が
1030×580 の灰色の仮画像**で、そこに「差し替え前提」と書いてあった。
**写真が無い会社ほど、ページの主役が「無い写真」になっていた。**

仮の画像そのものは消していない（D-242）。**消したのは最初の画面に置くことだけ**で、
写真の帯にはこれまでどおり出る。判定は `analysis.hasRealPhotos` と同じ規則（D-174）。

**空白にはならない。** 最初の画面は 会社名 → 導入（24px）→ 条件の4枡 で、
2で導入が一段上がったぶん、**仮画像が無くても頭が締まる。**
新しい `AssetSubject` も `Presentation` も足していない。

---

# 実装記録（第7段階②・幅だけ）

アドバイザーの判断で **4（中身の幅を4段階に）だけ**を実装した。
**5（2つ目の山）・6（型に段の強さ）・スマホのタイポ再設計・モーション・装飾は触っていない。**

## 現状調査でわかったこと

**4段はもうあった。使われていなかっただけである。**

`Width` は前から `narrow / normal / wide / full` の4段で、CSSにも3行書いてあった。
全15型・168帯を実測した結果：

```
narrow 81本 ／ normal **1本** ／ wide 80本 ／ full 6本
.inner の幅の種類：760 / 1070 / 1280 の3つだけ
```

原因は2つ。

1. **`wide`（1440px）も `full`（1680px）も、使える幅より大きい。**
   画面1280pxで脇のメニュー210pxを引くと中身は1070pxなので、
   **`normal` `wide` `full` の3つが同じ1070pxに潰れていた。** 上限が一度も効いていない。
2. **幅を帯の種類ごとに手で書いていた。**
   同じ見せ方なのに帯によって幅が違い（工程が 476 / 634 / 702 / 712 / 720 / 782 / 864 / 1091 / 1240px）、
   **目が「この幅はこういう話」と覚えられない。**

項目名と本文の距離も測った：**244 / 318 / 376px**（33本）。

## やったこと

### ① 幅を「見せ方」から決める（`system/width.ts`）

```
narrow … prose / longform / quote / timeline / process   縦に読むもの
normal … list / chips / comparison                        読むが横にも少し広がるもの
wide   … spec / cardGrid                                  突き合わせて読むもの
full   … largeNumber / fullWidth                          見るもの
```

**同じ見せ方なら、いつでも同じ幅。** 6社 × 全15勝ち筋で組み直して確かめている。

**型ごとの偏りは、この表では作らない。** 「精密加工は広め、職人は狭め」は
`PLAYBOOK` が型ごとに違う見せ方を好むので**自然に出る**。
幅の表に型を持ち込むと、同じ見せ方が型によって違う幅になり、覚えられなくなる。
（`Direction` 型にパラメータは足していない。）

### ② 上限を、使える幅の内側に置き直した

| | 前 | 後 | 1280pxでの中身 |
|---|---|---|---|
| narrow | 760px | **720px** | 680px（`--measure` と揃う） |
| normal | （無し＝1200） | **900px** | 860px |
| wide | 1440px | （無し＝既定） | 1030px |
| full | 1680px | **上限なし＋左右の余白を捨てる** | 1070px |

**スマホでは4段が1段になる**（画面幅いっぱいが既定）。差が出るのは `full` だけで、
そこだけ左右の余白を捨てる。

### ③ 横置きの軸線を細くした

```
前 minmax(200px, 1fr) 2.6fr  gap 44px   → 左の列 274px、項目名と本文が 318px 離れる
後 minmax(110px, 170px) 1fr  gap 32px   → 左の列 170px
```

第7段階①で項目名を14pxにしたので、274pxの列は**中身の8割が空**だった。
項目名が「軸線」ではなく「置き去り」に見えていた。

### ④ 単調さを避ける規則に、1つ条件を足した

幅を見せ方から引き直したら `narrow` の帯が増え、
**「同じ組み方が3つ続いたら3つ目を変える」規則が、2段落の説明文を横置きに移した。**
見出しが 33px から 14px の項目名に落ち、**読ませたい帯の見出しがいちばん小さい文字**になった。
**縦に読む帯（`narrow`）は動かさない**を条件に足した。
これは幅の変更の後始末であって、5・6には触れていない。

## 結果（実測）

### 幅の段（松原精機・PC 1280px・全ページ）

| 段 | 本数 | 中身の幅 | 中身の左端 | 見せ方 |
|---|---|---|---|---|
| narrow | 11 | **680px** | 405 | quote / process / prose / longform |
| normal | 4 | **860px**（横置き時 658） | 315 / 517 | chips / list / comparison |
| wide | 7 | **1030px**（横置き時 828） | 230 / 432 | spec / cardGrid |
| full | 5 | **1070px**（横置き時 868） | **210**（帯の端） | fullWidth |

**4段が4つの別の幅として出ている。** 中身の左端も 210 / 230 / 315 / 405 の4箇所になった
（前は 230 と 385 の2箇所）。

### 全15型（写真0枚・PC）

| | 前 | 後 |
|---|---|---|
| `.inner` の幅の種類 | **3**（760 / 1070 / 1280） | **4**（720 / 900 / 1070 / 1200） |
| 4段の使われ方 | narrow 81 / normal **1** / wide 80 / full 6 | narrow 117 / normal 12 / wide 39 / full ※ |
| 項目名と本文の距離 | **244 / 318 / 376px** | **202px（1種類）** |
| 中身の実効幅の種類 | 13 | 11 |
| 横スクロール | なし | **なし**（PC・スマホとも） |

※ この計測の会社は写真0枚・大きな数字なしなので `full` が出ない。
実案件（松原精機）では5本出ている（上表）。

### 悪化したもの・変わらなかったもの

- **`wide` と `full` の差は 40px（左右の余白ぶん）しかない。**
  「もっと広い」ではなく「**帯の端まで届く**」が `full` の意味である。
  写真が端に接するかどうかの差なので、**文字だけの帯では差が出ない。**
- **スマホでは4段が1段になる**（画面幅いっぱいが既定）。差が出るのは `full` だけで、
  そこだけ左右の余白を捨てる。**これは縮小ではなく、別の定義である。**
- 中身の実効幅の種類は 13 → 11 に**減った**。同じ見せ方が同じ幅になったぶん、
  ばらつきが減っている。**種類の多さは目的ではない。**

---

# 実装記録（第7段階③〜⑥）

`06f335d` `79b0fd9` ／ D-357〜360

## ③ 描けない山を立てない（D-357）

**きっかけは、こちらの計測ミスだった。** 「汎用9方向のトップに山が立たない」と報告したが、
汎用の会社は公開ゲートを通って `projects/*/site` に書き出されるのに、
検査が `site-draft/` だけを見ていた。**404を「帯0本・山0個」と数えていた。**
測り直すと汎用3社 × 汎用9方向＝**27/27で山あり**。

ただし測り直す過程で本物の欠陥が出た。**「条件を壁に」（`spec`）が実際に大きくするのは表だけ**
なのに、語彙の表は札束（`cardGrid`）と札（`chips`）にも許していた。
該当は**汎用34帯・製造業22帯**で、**汎用トップの主役の山の多くがこれ**だった
——余白と見出しだけが大きくなり、主役であるはずの札は変わらない（D-320で禁じた形）。

第6.5段階の2つ目の山に当てた規則（D-356）を主役にも当て、`draws()` として1箇所にまとめた。

| | 前 | 後 |
|---|---|---|
| 山0のページ | 225 | 240 |
| 山2のページ | 38 | 13 |
| **トップで山0** | **0** | **0** |

## ④ 白抜きの地の上の見出し（D-358）

全15型198画面8419箇所を測って、**読めない文字が14箇所**見つかった。

```
「取り扱い」        1.00:1   文字も地も同じ色。見出しが消えていた
「代表より」        1.01:1
料金の目安         1.08:1
色地の表のセル      3.83:1
```

原因は毎回**重なりの勝ち負け**である。見出しの色は役割・組み方・強さ・飾りごとに散らばっていて、
**どれも「白抜きの地では白に倒す」より強い。** 第7段階①で項目名に `color: var(--ink)` と
書いたのが直接の引き金だった（`inherit` に直した）。**D-329 と同じ見落としの2度目。**

追いかけるのをやめ、**①項目名は色を継ぐ ②地の色から決める規則を節の最後に置く**の2本にした。
**色地は「暗い地」ではない**ことも分かった——白を重ねると札が明るくなって白い文字に近づくので、
色地では黒を重ねて沈める。

## ⑤ 汎用トップの山が1種類に揃う件（現状維持）

汎用3社とも、**主役の帯で描ける山は `statement` 1種類しかない**（`offerings` は札束、
`conditions` が無い）。tone の順を入れ替えても、種類を外・帯を内にしても**1ページも変わらない**
（実測）。残るレバーは「主役の帯から山を選ぶ」をやめることだが、これは D-302 に反する。

**規則をいじって差を作ると、材料に無い差を発明することになる。** 現状維持とした。

## ⑥ 段の強さ（D-360）

型は15あるのに、**見出しの大きさは15方向とも同じ数字**だった（段差は 1.96倍×12方向と
1.85倍×3方向の2種類だけ）。しかも `textSize: large` を選んだ型ほど**段差が浅くなる**。

余白の強さは `mood.section`（60/72/88px）として**既にあった**ので、2つ目は作らない。
足したのは `mood.typeScale` 1つだけで、**余白と逆向き**にした。

| mood | 帯余白 | 文字 | 1280pxでの段差 |
|---|---|---|---|
| かたい | 60px | ×1.12 | **2.19倍** |
| 標準 | 72px | ×1.0 | 1.96倍 |
| やわらかい | 88px | ×0.92 | 1.80倍 |

**強さは文字か余白のどちらかで出す。両方を同時に上げると、ただ大きいだけになる。**
かけるのは見出しの役割（`head`）だけで、`min`（スマホの実寸）は動かさない（D-348）。
結果、段差は2種類 → **5種類**（2.19／1.96／1.85／1.80／1.70倍）、
**スマホは全方向22pxのまま。**

## 第7段階の最終確認（594画面）

15方向 × PC/スマホ × 写真0/あり × 全ページ。横スクロール0・空帯0・山は1〜2個・
内容欠落0・2回ビルドの差分0・sitemap全URL実在・コントラスト8419箇所すべて4.5:1以上。
