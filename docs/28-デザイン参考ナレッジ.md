# デザイン参考ナレッジ

**目的：** 「格好いいサイト」を眺めて終わりにせず、**我々の語彙（surface / layout / hero / motif / media / motion / presentation）に翻訳して積む。**
翻訳できないものは、参考にならない。テンプレートは1つしか持たない（D-096）ので、
**1案件だけのために作った見た目は、そもそも入れられない。**

---

## 記録のルール

> **見ていないものを書かない。**
>
> 各エントリには「**誰が・いつ・何を見て**書いたか」を必ず入れる。
> 画面を見ずに属性だけを見て「実測」と書き、2件を誤報した前科がある（D-228）。
> **推測で書いた行は、あとから区別がつかなくなる。**

各参考サイトについて、**次の9項目を必ず埋める。** 埋まらない項目は「未確認」と書く。

| 項目 | 何を見るか |
|---|---|
| 1. 写真の依存度 | **その絵は写真が無くても成立するか。** 全画面写真が主役なら、我々には移植できない |
| 2. 明暗 | 暗い面を使っているか。使っているなら、どの帯で |
| 3. 型の大きさ | 見出し・本文のpx。とくに**いちばん大きい文字** |
| 4. 幅 | コンテナの最大幅。全幅の帯があるか |
| 5. 縦のリズム | 帯と帯の間隔 |
| 6. 色数 | アクセントは何色か。無彩色との比 |
| 7. 動き | 何が動くか。止めても成立するか |
| 8. 情報表現 | 我々の12個（prose/list/chips/cardGrid/largeNumber/spec/comparison/timeline/process/quote/longform/fullWidth）のどれに当たるか。**当たらないものは、増やす価値があるか** |
| 9. 材料 | その見せ方に必要な材料を、**我々の取材74項目で集められるか。** 集められないなら絵に描いた餅 |

最後に必ず1行：**「これを我々の案件で再現するには、何が足りないか」**

---

## いまの我々の値（比較の基準）

2026-09-14 実測。`tool/site-template/src/styles/site.css` より。

| | 値 |
|---|---|
| 本文 | 17px ／ 行間1.85 ／ 1行 40em |
| コンテナ最大幅 | **1040px**（帯は narrow 760 / normal 1040 / wide 1280 / full 1440） |
| 帯の縦余白 | **52px**（`--section`） |
| 見出し | h1 31px（ヒーロー36px）／ h2 24px |
| いちばん大きい文字 | ヒーローの数字 `clamp(34px, 6vw, 68px)` |
| 面 | plain / soft / accent / grid / rule / paper の6種。**暗い面は無し**（D-199） |
| 動き | `prefers-reduced-motion: no-preference` のときだけ。素のHTMLは最初から見えている |
| 写真 | **実写が無ければ写真中心の型を選ばない**（D-174）。松原精機はいま実写0枚 |

---

## 参考001：industrialsitedesign.com

- **提示**：アドバイザー（2026-09-14）
- **URL**：https://www.industrialsitedesign.com/
- **状態**：**未取得。**この作業環境のネットワーク制限で遮断された

```
$ curl https://www.industrialsitedesign.com/
curl: (56) CONNECT tunnel failed, response 403
```

クラウドセッションの外向き通信は環境の **Network access** で決まっており、
いまは既定の **Trusted**（パッケージレジストリ等の許可リストのみ）になっている。

**中身を見ていないので、9項目は1つも書かない。**
見ずに genre の一般論を書くと、それが「この参考サイトの特徴」として残ってしまう。

### 取得する方法（どれか1つ）

1. **環境の Network access を広げる**
   claude.ai/code のメッセージ欄の上にある雲アイコン → 環境の歯車 →
   **Network access** を **Full**、または **Custom** にしてドメインを追加
2. **こちらに貼る**：ページのHTML／CSS、またはスクリーンショット。
   **画像でも読み取れる。** ネットワークが要らないので確実
3. **口頭で**：どの部分が良いと思ったか（ヒーロー／余白／写真の使い方／色）。
   **「どこが良いか」は人によって違うので、これがいちばん確実な情報になる**

---

## 参考002〜005：再取得の試み（2026-09-14・2回目）

- **提示**：アドバイザー（2026-09-14）
- **状態**：**4件とも未取得。**前回と同じく、この作業環境のネットワーク制限で遮断された

| | URL | 結果 |
|---|---|---|
| 参考002 | https://www.industrialsitedesign.com/ | 未取得（CONNECT 403） |
| 参考003 | https://www.industrialsitedesign.com/sites | 未取得（CONNECT 403） |
| 参考004 | https://webflow.com/made-in-webflow/industrial%20manufacturing | 未取得（CONNECT 403） |
| 参考005 | https://webflow.com/made-in-webflow/manufacturing | 未取得（CONNECT 403） |

```
$ curl https://www.industrialsitedesign.com/
curl: (56) CONNECT tunnel failed, response 403
$ curl https://www.industrialsitedesign.com/sites
curl: (56) CONNECT tunnel failed, response 403
$ curl "https://webflow.com/made-in-webflow/industrial%20manufacturing"
curl: (56) CONNECT tunnel failed, response 403
$ curl "https://webflow.com/made-in-webflow/manufacturing"
curl: (56) CONNECT tunnel failed, response 403
```

プロキシ自身の記録にも、拒否したのはポリシー判断だと出ている。

```
$ curl "$HTTPS_PROXY/__agentproxy/status"
"recentRelayFailures": [
  { "kind": "connect_rejected",
    "detail": "gateway answered 403 to CONNECT (policy denial or upstream failure)",
    "host": "www.industrialsitedesign.com:443" },
  { "kind": "connect_rejected",
    "detail": "gateway answered 403 to CONNECT (policy denial or upstream failure)",
    "host": "webflow.com:443" }
]
```

ブラウザ経由の取得（WebFetch）も同じ壁で止まった。

```
WebFetch https://www.industrialsitedesign.com/
→ EGRESS_BLOCKED: blocked by the network egress proxy
WebFetch https://webflow.com/made-in-webflow/manufacturing
→ EGRESS_BLOCKED: blocked by the network egress proxy
```

リポジトリ内にこれらのページの控え（HTML・CSS・スクリーンショット）は無い。
`grep -ril "industrialsitedesign|made-in-webflow"` の結果はこの28番の文書だけ。

### 9項目は1つも書かない

**HTMLもCSSも1バイトも取れていない。**
したがって 1写真の依存度／2明暗／3型の大きさ／4幅／5縦のリズム／6色数／7動き／
8情報表現／9材料 の**9項目すべてが「未取得」**であり、ここには書かない。

見出しのpx、コンテナ幅、帯の縦余白、色の定義は、**CSSを読まなければ実数にならない。**
読まずに書いた数字は推測であり、あとから実測と区別がつかなくなる（D-228）。
**我々の実測値（本文17px／コンテナ1040px／帯の余白52px／いちばん大きい文字
`clamp(34px, 6vw, 68px)`）と並べる相手が、いま存在しない。**

**これを我々の案件で再現するには、何が足りないか：**
再現の可否を判断する材料そのもの（ページのHTMLとCSS、または画面）が無い。足りないのは判断材料であって、実装力ではない。

### 4件に共通する要素・我々の語彙に無いもの

**書けない。**4件とも見ていないので、「共通して出てくる要素」は
1件も観測できていない。ここで industrial 系サイト一般の話を書けば、それは
**この4件の観測結果として残ってしまう。**参考001と同じ理由で空欄にする。

### 遮断を回避する手当てはしていない

指示どおり、コードにも設定にも手を入れていない。
プロキシの無効化、TLS 検証の解除、別経路の探索は、いずれも行っていない。

取得の方法は参考001に書いた3つのまま（環境の Network access を Full / Custom に広げる、
HTML・CSS・スクリーンショットを貼る、口頭でどこが良かったかを伝える）。
**3つ目がいちばん確実で、いちばん早い。**

---

## まだ参考を積む前に決めること

この2つが決まらないと、どの参考も「移植できない絵」で終わる。

### 保留① 実写を前提にするか（2026-09-14／アドバイザーが友人と相談して決定）

Industrial 系の見た目は、**その大半が写真で持っている。**
いま松原精機は仮のSVGが9枚で実写0枚。検証用の3社も0枚。

前提にするなら、**取材時の撮影を工程に入れる**必要がある（訪問1回で2〜3時間、
機材、載せてよい写真かの確認、98万円に含めるか別料金か）。
前提にしないなら、**写真が無くても成立する方向**（大きな文字・罫・地紋・図版）に
振り切ることになり、到達点は参考サイト群とは別の絵になる。

**2026-09-14：アドバイザーが友人と相談して決める。** それまでは写真ゼロでも
成立する前提を崩さない（D-099）。

### 決定済み② 暗い面を解禁する（2026-09-14・D-230）

D-199 は「暗い背景は作らない」。理由は**コントラスト検査が実質2倍になり、
6配色すべてに暗色面の定義が要る。効果より手数が勝つ**。
Industrial 系は暗い面を使うことが多く、**参考例とこの決定は正面からぶつかる。**

**解禁した。** ただし**新しい色は1つも足していない。**
暗い地は `--ink`（本文の色）をそのまま使う。白文字で 15.9〜18.4:1（6配色すべて）。
検査は84件→102件。`--accent` を暗い地の上の文字に使うと 1.1〜2.9:1 で読めないため、
**使用禁止であることも検査に書いた**（読めてしまったら前提が変わったということ）。

使うのは **量産・設備**と**製品・開発**の2つの型で、**1ページに1回、lead の帯にだけ**（D-231）。
残る4つの製造業向けの型（標準・精密加工・老舗職人・設計技術）は明るいまま。
**どの型で使うかは、参考サイトを見てから広げる。**
