# 38. 保留中の作業

**始めたが、何かを待って止めているもの**の一覧です。
待っている相手（社長／KOBO側）と、**何が来れば再開できるか**を書きます。
片付いたら、その行を消して `04-意思決定ログ` に結果を残します（ここは作業の控えで、記録ではありません）。

最終更新：2026-09-24

---

## 0. 最初の実案件の原稿確認までに要るもの（KOBO側）

いまは無し。0-1（型を変えて2〜3通り書き出す道具）・0-2（文字の大きさを原稿のご確認で選んでいただく手順）は **D-472** で片付いた。
使い方は `06-取材台本.md` の「原稿のご確認のときに決めること」。

## A. 社長の手元のPCが要るもの（2026-09-23 から保留）

| # | やること | 待っているもの | 再開の手順 |
|---|---|---|---|
| A-1 | **APIキーを置く** | 社長のPC | `cd Eclipse-restudy/tool` → `git pull origin claude/web-production-company-advisory-5ael1c` → `cp .env.example .env` → `.env` の `ANTHROPIC_API_KEY=` の後ろにキーを書く（D-468） |
| A-2 | **掲載文のAI下書きを動かす** | A-1 | `npm run check:api`（キーの確認・1円未満）→ `npm run webtext -- matsubara-seiki --draft`（数分）→ **`projects/matsubara-seiki/draft/webtext.json` をそのまま送っていただく**。取り込み（`--apply`）は、KOBO側が下書きを読んで型と指示を直してから（D-466） |
| A-3 | **松原精機のロゴの欄** | 社長のPC | ローカルの `projects/matsubara-seiki/project.json` の `basics` に `"logoIncludesName": true` を入れる。入れなくても結果は同じ（聞けていない間は文字を出さない・D-462） |

## B. 社長のお答え待ち

| # | 伺っていること | なぜ要るか |
|---|---|---|
| B-1 | **生成ビジュアル3枚を作ったサービス名とプラン** | 来歴の欄（`provenance.provider`）。未確認の間は公開を止めている（D-465） |
| B-2 | **そのサービスの規約で商用利用できるか** | 来歴の欄（`provenance.commercialUse`）。同上 |
| B-3 | **画像生成をAPIで行うなら、どのサービスか** | 呼び方も規約もサービスで違う。いまは画像生成のAPIを呼ぶコードが1行も無く、キーを置いても何も起きない（D-468） |
| B-4 | **代表挨拶に「若手の加工オペレーターを探しています」が出ている件** | KOBO側の書き出しでは再現できていない。**実際に見えている文をそのまま引用**していただきたい |
| B-5 | **トップの絵の無い区間（PC 2809px・本文の78%）をどうするか** | 案A：加工事例の写真（3枚ある）をトップに出す帯を足す／案B：トップ中盤用の生成ビジュアルを1枚足す／案C：足さない。写真を描く帯が「1ページに1つ・分類はページで決まる」作りなので、Aは構造の変更になる |
| B-6 | **撮影を商品に含めるか**（γ） | B-5 の裏返し。実写の枚数が、トップ・強み・技術・加工事例の見え方を決める |

## C. KOBO側でできるが、急がないもの

| # | 内容 | メモ |
|---|---|---|
| C-1 | 強み・技術の1本目の帯（他社様で難しいと言われた案件）に絵が無い | 注文書が4件→3件になり、`peak` が「実写があるなら実写に譲る」で落ちた。置くならルールを変えずにプロンプトをもう1本出す |
| C-2 | スマホの最初の画面で、板の白い光の線が文字の後ろを横切る | 読めるがPCよりにぎやか。スマホでは文字を避ける場所が取れない |
| C-3 | 掲載文の字数の上限（最初の画面と山以外）は暫定 | 実画面を見て決め直す（D-466） |
| C-5 | **配色・書体など9つの軸を直す画面が無い**（D-470・D-471 で取材から外した） | いまは型を押すとまとめて決まる。書き出したサイトを見て軸を1つずつ直すときは `project.json` の `theme` を直接書く。**頻繁に要るようになったら、制作側の画面を作る** |
| C-4 | トップの `/strengths/` へのリンクが2本並ぶ | D-410「参照には本体への導線を必ず出す」の結果。減らすなら D-410 を変えることになる |
