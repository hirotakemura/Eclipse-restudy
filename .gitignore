# QAが撮り直す画面。**再生成できるものをGitに置かない**（D-328）
#
# `npm run qa:assets` / `npm run qa:visual` は毎回18MBのPNGを書き出す。
# 実行のたびに履歴が太るうえ、**画面は手元で見るもの**で、履歴に積む意味がない。
# 検査票（人が見て判断した記録）と再現手順は残す。
#
# 取材フォームなどの説明用キャプチャ（captures 直下）は、撮り直さないので残す。
docs/assets/captures/*/*.png
