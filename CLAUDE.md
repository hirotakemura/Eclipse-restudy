# KOBO — 全担当に共通の約束

**ここには、どの担当でも毎回守ることだけを書く。** 担当ごとの手順は Skill に分けてある（D-473）。

## 担当と Skill

| 担当 | 呼び方 | 受け持ち |
|---|---|---|
| 営業 | `/eigyo` | 見込み客・提案・契約・**取材の進め方・原稿のご確認の進め方**・保守の窓口 |
| KOBO | `/kobo` | 取材の入力画面・案件データ・掲載文の流れ（AIの下書き→検査→人の承認）・事実の照合・APIキー |
| WEB制作 | `/web` | 書き出し・見た目（型・文字・余白・写真・生成ビジュアル）・見比べ・公開・移管 |

- セッションの最初に、**担当名とやること**を書いて始める（例：`/web 松原精機の会社概要の写真を直す`）。
- 担当の外の仕事が出てきたら、**その場で手を広げない。** `docs/38-保留中の作業.md` の該当する担当の節に書き、報告で伝える。
- 担当どうしの受け渡し：営業 →（取材で入力した案件データ）→ KOBO →（承認済みの掲載文）→ WEB制作 →（見比べ画面・公開したサイト）→ 営業。

## 呼び方と役割

- **会話の相手（オーナー）を「社長」と呼ぶ。** 意思決定ログでは D-386（2026-09-17）以降この呼び方で、それより前の「アドバイザー」も同じ人を指す。
  `README.md` の体制表と `docs/06` の一部では、Claude を「社長（私）」と書いている箇所が残っている（古い呼び方。書き換えはしない）。
- Claude は制作の実務（設計・実装・原稿・検査）を担う。**契約・入金・面談・現地取材は人間側**が行う。
- **決めるのは社長。** 案を出し、承認を得てから進める。デザインの変更は社長の承認なしに行わない（2026-09-17・社長のご指示）。

## 見た目は、画面を見るまで合格にしない（2026-09-17・社長のご指示）

コードの上で正しく置かれていることと、人が実際の画面で認識できることは別の話である。
- 見た目にかかわる変更は、**PC・SPのキャプチャを撮って目で見るまで「○」にしない。** 撮れなければ「**未確認**」と書く。
- 社長と同じ画面を見られないときは、推測で「問題なし」と言わず、キャプチャを送っていただく。
- 手順・禁止する言い方・報告の形は `/web`（`.claude/skills/web/SKILL.md`）にある。**見た目に触れる担当は、必ずそれに従う。**

## 赤いときは、まずコードではなく測り方を疑う

この案件では、**赤く出た検査の多くが測り方の誤り**だった（D-392・D-399・D-400 ほか）。
検査が赤いとき、実装を直す前に「その測り方は、測りたいものを測っているか」を確かめる。
**緑のときも同じ**——直したものを一度戻して、検査が赤くなることを確かめる（D-472）。

## 決めたことは `docs/04-意思決定ログ.md` に残す

- D番号・日付・決めたこと・理由・**撤回の条件**まで書く。**古い判断は消さない**（撤回したことも記録する）。
- 追記したら `docs/README.md` の「D-001〜D-○○○」も上げる（`npm run test:skills` がずれを見る）。
- 書き写した表は必ずいつかずれるので、同じ数字を2箇所に書いたら、**ずれたら落ちる検査**を必ず添える（D-197）。
- **docs の番号は変えない。** コードから番号で参照している（D-364）。分類は `docs/README.md` の中だけで行う。
- 待っているもの・後回しにしたものは `docs/38-保留中の作業.md` の担当の節へ。片付いたら行を消して、結果をログに残す。

## お客様の情報とAPIキー

- **APIキーが無い場合は、コードを変更して回避しない。APIキーが無いことを明記して止める**（社長のご指示）。
- キーは `tool/.env` にだけ置く（Git に入らない・D-468）。**会話にキーを貼っていただかない。**
- 案件データ `tool/projects/*` は Git に入らない。**お客様の情報を Git に載せない。** 検証には架空の `tool/fixtures/` を使う（D-185）。

## Git とセッション

- 作業ブランチは `claude/web-production-company-advisory-5ael1c`（全担当で同じ）。ほかのブランチに push しない。
- **同じ時間に2つのセッションで作業するなら、触るファイルが重ならない組み合わせだけ**にする。
  `docs/04-意思決定ログ.md`・`docs/README.md`・`tool/package.json` はどの担当も書くので、**始める前に `git pull`、書いたらすぐ commit・push** する。
  D番号がぶつかったら、後から push する側が番号を振り直す（`npm run test:skills` が重複を見つける）。
- **会話が長くなり、要約が始まったら、新しいセッションに移る合図。** 引き継ぎは会話ではなく、ログと `docs/38` で行う。
